# Gitleaks ignore list — known false positives (commit SHAs)
# Format: one SHA per line, comments with # allowed.
#
# This file is used when scanning git history (--log). With --source=. (HEAD only),
# inline `// gitleaks:allow` annotations take precedence instead.
