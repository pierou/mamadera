# Gitleaks ignore list — known false positives (secret fingerprints)
#
# Entries here must be the SHA-1 *fingerprint of the secret value* that
# gitleaks reports (not a commit SHA). Only add one when a specific secret
# already in git history must be exempted from history scans.
#
# Prefer inline annotations in source files instead:
#   // gitleaks:allow
# e.g. the test-only mock key in
#   test/core/services/encryption_service_test.dart
#
# Currently empty — no exemptions needed.
